import React from 'react';
import ReactDOM from 'react-dom/client';
import MainPage from './main_page';

const main_root =  ReactDOM.createRoot(document.getElementById('main_page_root'));
main_root.render(
    <MainPage/>
)